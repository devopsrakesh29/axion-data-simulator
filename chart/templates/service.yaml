apiVersion: v1
kind: Service
metadata:
  name: {{ include "axion-data-simulator.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "axion-data-simulator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "axion-data-simulator.selectorLabels" . | nindent 4 }}
